# CONF
source "$ZDOTDIR/core/env.zsh"
source "$ZDOTDIR/core/options.zsh"
source "$ZDOTDIR/core/inits.zsh"
source "$ZDOTDIR/core/completions.zsh"
source "$ZDOTDIR/core/keybindings.zsh"

for file in "$ZDOTDIR"/modules/*.zsh(N); do
  source "$file"
done
unset file
